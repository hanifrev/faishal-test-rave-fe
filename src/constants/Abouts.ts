export interface AboutSectionType {
  mainTitle: string
  sightsHighlight: {
    title: string
    list: string[]
  }
  travelHighlight: {
    title: string
    description: string
    CTA: string
    list: string[]
  }
}
